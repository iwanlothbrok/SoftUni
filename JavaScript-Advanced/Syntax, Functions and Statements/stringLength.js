
    function sum(firstInput,secondInput,thirdInput){
        let sumOfTheLength = firstInput.length + secondInput.length + thirdInput.length;
        console.log(sumOfTheLength);
        console.log(Math.floor(sumOfTheLength / 3));
    }

sum('chocolate', 'ice cream', 'cake');